已完成以下工作：

## 生成的文件

### 1. architecture.md（系统架构设计文档）
- 版本：v1.0.0（已冻结）
- 包含完整的技术架构图、数据模型设计（12个表）、API 设计、服务设计、配置管理、目录结构、部署架构、安全设计
- 所有表结构使用统一数据库 `./data/qms_nexus.db`

### 2. todo.md（任务清单）
- 版本：v1.0.0
- 按优先级分为 4 个阶段，共 15 个任务
- 每个任务包含：优先级、文件、当前状态、详细需求、依赖关系、验收标准
- 可直接将任务提示词发给 AI 完成

## 任务阶段划分

**阶段一（基础设施）**：数据库架构 → 配置管理 → 标签存储迁移 → 文档存储迁移
**阶段二（核心功能）**：系统日志 → 问答日志 → 反馈功能 → 修正库功能
**阶段三（集成优化）**：LLM 集成 → 服务重启 → 备份恢复
**阶段四（增强功能）**：API Key → 统计功能 → 用户认证 → 文档预览

## 架构关键决策

1. **统一数据库**：使用 SQLite 单文件 `./data/qms_nexus.db`，替代现有的多个分散数据库
2. **保留现有服务**：auth.py、correction_service.py、knowledge_base.py 已有基础实现，只需检查完善
3. **删除冗余**：无（所有需求都已保留）
4. **新增模块**：database.py、tag_store.py、document_store.py、log_store.py、chat_logger.py、feedback_store.py、stats_service.py、backup_manager.py

## 后续执行建议

按顺序执行任务 1-4（阶段一），然后可并行执行任务 5-8（阶段二），最后按需执行阶段三和四。